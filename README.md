# Railway Navigator 🚆

Проект по визуализации алгоритмов на графах для транспортных сетей: граф строится из входных данных, маршруты подсвечиваются, а backend вычисляет изолированные зоны и метрики.

## ✨ Возможности
- визуализация транспортного графа в браузере (без внешних библиотек)
- подсветка заданного маршрута по шагам
- отчет по параметрам модели (чувствительность, штрафы пересадок)
- вывод изолированных зон для каждого вида транспорта (через backend)

## 🚀 Быстрый старт (frontend + backend)
1) Сборка backend:
```bash
cmake -S . -B build
cmake --build build
```
Готовый бинарник: `build/backend/railway_navigator`.

2) Запуск локального сервера:
```bash
python3 server.py
```
Открыть в браузере: `http://127.0.0.1:8000/`.

## 🧩 Запуск без сервера
Можно открыть `frontend/index.html` напрямую. В этом режиме:
- граф строится и подсветка работает
- изолированные зоны не обновляются (нет backend)

## 📄 Формат входных данных
Вводится единым блоком чисел в таком порядке:
```
N M
sens0 sens1 sens2
trans00 trans01 trans02
trans10 trans11 trans12
trans20 trans21 trans22
station_transfer[1..N]
M lines:
  u v mode base_time load
Q
Q queries:
  start T k  (затем T целевых станций)
```

Пояснения:
- `N` — число станций, `M` — число рёбер.
- `sens0..sens2` — чувствительность для режимов (0=metro, 1=bus, 2=rail).
- `transXY` — матрица штрафов пересадки 3x3.
- `station_transfer` — локальный штраф пересадки для каждой станции (N чисел).
- `u v mode base_time load` — ребро графа.
  - `mode`: 0=metro, 1=bus, 2=rail
  - `base_time` — базовое время
  - `load` — нагрузка (0..1)
- `Q` — число запросов (для backend). Если хотите только визуализацию, можно указать `Q=0`.

## 🧭 Маршрут для подсветки
Формат строки маршрута:
```
1-[metro]->2 2-[bus]->3 3-[rail]->4
```
Важно: режим должен совпадать с `mode` ребра в данных.

## 🧪 Тестовые данные
Тестовые сценарии лежат в `input.txt` и содержат несколько блоков с разными графами.

Как использовать:
- скопируйте **только** блок между `BEGIN INPUT` и `END INPUT` в поле ввода
- строку `ROUTE:` вставьте в поле маршрута

Пример:
```
BEGIN INPUT
...числовые данные...
END INPUT
ROUTE: 1-[metro]->2 2-[rail]->4
```

## 👥 Команда и задачи
| Участник | Зона ответственности |
| --- | --- |
| Дмитрий | DFS (ж/д), Дейкстера, тесты для Дейкстры |
| Николай | DFS (автобусы), изолированные зоны, |
| Богдан | DFS (метро/общий граф), вывод маршрутов, документация |
| Богдан Гришков | QuickSort, документация|
| Мухаммед | Архитектура проекта, интеграция, frontend |

## 💼 Вклад участников (итог)
- Дмитрий — DFS (ж/д), Дейкстера, тесты Дейкстры
- Николай — DFS (автобусы), изолированные зоны
- Богдан — DFS (метро/общий граф), вывод маршрутов, документация
- Богдан Гришков — QuickSort, документация
- Мухаммед(Тимлид) — архитектура проекта, интеграция, frontend, тесты для DFS

## 📁 Структура проекта
- `backend/` — C++ исходники (алгоритмы и логика)
- `frontend/` — веб‑интерфейс (HTML/CSS/JS)
- `server.py` — локальный Python‑сервер для связи frontend ↔ backend
- `input.txt` — набор тестовых сценариев
- `docs/` — документация
- `build/` — директория сборки

## 🧪 Сборка тестов
Тесты по умолчанию **не собираются и не запускаются**. Чтобы собрать тесты:
```bash
cmake -S . -B build -DBUILD_TESTS=ON
cmake --build build
```
Запуск тестов выполняется вручную.
